<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.claytonandrade.lanchonetemodelo">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/menu_patrocinadores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Cardapio"
            android:label="@string/title_activity_cardapio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Bebidas"
            android:label="@string/title_activity_bebidas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Pedido"
            android:label="@string/title_activity_pedido"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Entrega"
            android:label="@string/title_activity_entrega"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Contato"
            android:label="@string/title_activity_contato"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="model.BebidaModel" />
        <activity android:name="model.Lanche" />
        <activity android:name=".AnuncioSimples" />
        <activity android:name=".AnuncioPersonalizado" />
        <activity android:name=".cards.CardCardapio" />
        <activity android:name=".cards.CardBebidas" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".OndeEstamos"
            android:label="@string/title_activity_onde_estamos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SetIngredientes"></activity>
    </application>

</manifest>